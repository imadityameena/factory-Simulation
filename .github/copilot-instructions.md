# Workspace Instructions

- Keep the application code in plain Java.
- Use Java Swing for the desktop UI.
- Keep the simulation deterministic when the same random seed is used.
- Prefer small, focused changes that preserve the current package structure.
